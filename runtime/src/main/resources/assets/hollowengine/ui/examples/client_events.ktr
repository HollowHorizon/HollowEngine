onClick(".action") {
    val button = if (it.id == "accept") "accept" else "decline"
    emit(struct { event: "clicked", button: button, mouse: it.mouseButton })
}

onEnter(".action") {
    emit(struct { event: "hover-enter", button: it.id })
}

onClose {
    emit(struct { event: "closed" })
}
